{
  "name": "elsoft-smart-contracts",
  "version": "1.0.0",
  "type": "commonjs",
  "scripts": {
    "compile": "hardhat compile --config hardhat.config.cjs",
    "deploy": "hardhat run scripts/deploy.cjs --network amoy --config hardhat.config.cjs"
  },
  "devDependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.6.1",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "ethers": "^6.1.0"
  }
}
